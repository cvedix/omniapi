edgeos-api (2026.0.1.50) unstable; urgency=medium
  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required

  * Package now includes CVEDIX SDK runtime bundled
  * No separate SDK installation required


  * Version 2026.0.1.1 release
  * Self-contained package with all dependencies bundled
  * Post-installation validation scripts
  * Version manifest tracking for reproducibility
  * Improved library bundling and RPATH configuration
  * Auto-increment version on build

 -- CVEDIX <support@cvedix.com>  Mon, 01 Jan 2026 12:00:00 +0000
