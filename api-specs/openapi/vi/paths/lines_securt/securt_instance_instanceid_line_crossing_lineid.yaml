# API endpoint: /v1/securt/instance/{instanceId}/line/crossing/{lineId}
# Tag: Lines SecuRT

/v1/securt/instance/{instanceId}/line/crossing/{lineId}:
  put:
    summary: Tạo crossing line với ID
    description: Tạo đường vượt (crossing line) với ID chỉ định cho instance SecuRT
    operationId: createCrossingLineWithId
    tags:
    - Lines SecuRT
    parameters:
    - name: instanceId
      in: path
      required: true
      schema:
        type: string
      description: Định danh instance
    - name: lineId
      in: path
      required: true
      schema:
        type: string
      description: Line ID
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/CrossingLineWrite'
    responses:
      '201':
        description: Crossing line created successfully
      '400':
        description: Yêu cầu không hợp lệ
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorResponse'
      '409':
        description: Line already exists
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorResponse'
      '404':
        description: Không tìm thấy instance
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorResponse'
      '500':
        description: Lỗi máy chủ
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorResponse'
  options:
    summary: CORS preflight cho crossing line với ID
    operationId: createCrossingLineWithIdOptions
    tags:
    - Lines SecuRT
    responses:
      '200':
        description: Phản hồi CORS preflight
