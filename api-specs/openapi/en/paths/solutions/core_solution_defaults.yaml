# API endpoint: /v1/core/solution/defaults
# Tag: Solutions

/v1/core/solution/defaults:
  get:
    summary: List default solutions
    description: Get list of all available default solutions from examples/default_solutions/
    operationId: listDefaultSolutions
    tags:
    - Solutions
    responses:
      '200':
        description: List of default solutions
        content:
          application/json:
            schema:
              type: object
              properties:
                solutions:
                  type: array
                  items:
                    type: object
                    properties:
                      id:
                        type: string
                        description: Solution ID
                      name:
                        type: string
                        description: Solution name
                      description:
                        type: string
                        description: Solution description
                total:
                  type: integer
                  description: Total number of default solutions
      '500':
        description: Server error
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorResponse'
  options:
    summary: CORS preflight for default solutions
    operationId: listDefaultSolutionsOptions
    tags:
    - Solutions
    responses:
      '200':
        description: CORS preflight response
