# API endpoint: /v1/core/node/build-solution
# Tag: Node

/v1/core/node/build-solution:
  post:
    summary: Build solution from nodes
    description: 'Build a solution configuration from selected pre-configured nodes.

      This endpoint allows creating a solution by combining multiple pre-configured nodes.
      The solution will be created with the specified nodes and their configurations.
      '
    operationId: buildSolutionFromNodes
    tags:
    - Node
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            required:
            - nodes
            properties:
              nodes:
                type: array
                items:
                  type: string
                description: Array of node IDs to include in the solution
                example:
                - node1
                - node2
              name:
                type: string
                description: Solution name
                example: My Custom Solution
              description:
                type: string
                description: Solution description
    responses:
      '201':
        description: Solution built successfully
        content:
          application/json:
            schema:
              type: object
              properties:
                solutionId:
                  type: string
                  description: Created solution ID
                message:
                  type: string
                  example: Solution built successfully
      '400':
        description: Invalid request
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorResponse'
      '500':
        description: Server error
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ErrorResponse'
  options:
    summary: CORS preflight for build solution
    operationId: buildSolutionOptions
    tags:
    - Node
    responses:
      '200':
        description: CORS preflight response
